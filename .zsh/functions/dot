dot() {
  if [[ $1 == edit ]]; then
    cd ~/dotfiles
    nvim .
  elif [[ $1 == lazygit ]]; then
    cd ~/dotfiles
    lazygit
  elif [[ $1 == pull ]]; then
    cd ~/dotfiles
    ./setup.sh
    tmux kill-session
  fi
}
